import tw from 'twin.macro';
import styled from 'styled-components';

export const StartingScreenStyled = styled.div(() => [
    tw`bg-yellow-500 h-full w-full flex place-content-center place-items-center`,
])